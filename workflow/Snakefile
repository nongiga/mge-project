import os, re, itertools, numpy, pandas
from os.path import basename, join

configfile: "workflow/config/config.yaml"

subworkflow mgefinder_workflow:
    workdir: os.getcwd()
    snakefile: "subworkflows/mgefinder.workflow.smk"
    configfile: "workflow/config/config.yaml"

subworkflow breseq_workflow:
    workdir: os.getcwd()
    snakefile: "subworkflows/breseq.workflow.smk"
    configfile: "workflow/config/config.yaml"

subworkflow pangenome_workflow:
    workdir: os.getcwd()
    snakefile: "subworkflows/pangenome.workflow.smk"
    configfile: "workflow/config/config.yaml"


rule all:
    input:
        mgefinder_workflow("mgefinder.done"),
        pangenome_workflow("pangenome.done"),
         breseq_workflow("breseq.done")
